GOAL
Explain how <feature / route / flow> works in this repo. CHANGE NOTHING.

DELIVERABLE — reply in the session. Do not open a PR, do not create a branch,
do not modify a single file.

1. Entry points, as file:line.
2. The render path from route to leaf component, naming each component.
3. Which primitives in components/ui/ are already in play.
4. Every file that would need to change to <the thing I am considering>.
5. Which design tokens in app/globals.css @theme that work would touch, and
   whether any new token would be required.
6. Anything surprising, tightly coupled, or likely to break.

NON-GOALS
- Do not modify any file.
- Do not create a branch or a PR.
- Do not propose a redesign. Describe what exists.

---

Run this when you don't know the anchors for the real brief. It costs one task
from quota and routinely saves a wasted 20-minute run. Paste its output straight
into the ANCHORS slot of the real brief.

Session settings: `requirePlanApproval: false`, and no `automationMode` — there
is nothing to create a PR from.
