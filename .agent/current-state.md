# Current State

- Pass 1 of 6-Pass Standing Order complete.
- Static export configured in `next.config.mjs` (output directory set to `out`).
- Unsplash remote patterns and unoptimized images enabled in `next.config.mjs`.
- Fonts updated in `app/layout.tsx` (Playfair Display for headings, Manrope for sans/body text).
- Globals font mappings configured in `app/globals.css`.
- Verification passed (`pnpm run verify`).
