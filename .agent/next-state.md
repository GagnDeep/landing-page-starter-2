# Next State

**Phase 7: Blog, FAQ, and Contact Pages**

Handoff:
Phase 6 is complete. Phase 7 will implement the interactive and informative secondary pages.
- Build `app/blog/page.tsx`, `app/faq/page.tsx`, and `app/contact/page.tsx`.
- The Contact page requires a functional form UI (client-side state).
- The FAQ page should utilize an expanded Shadcn Accordion setup.
- The Blog page requires grid layouts for articles.
- Consume content strictly from `content/blog.ts`, `content/faq.ts`, and `content/contact.ts`.
