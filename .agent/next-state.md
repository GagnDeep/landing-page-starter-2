# Next State

- Execute Pass 4 of the build order as described in `docs/BUILD-LOG.md`.
- Implement Services/Features section.
- Use `ImageSlot` from `lib/images.ts` for any images needed.
- Extend `content/index.ts` with structured text for services.
- Ensure all constraints in `AGENTS.md` and the standing order are followed.
- Update `docs/BUILD-LOG.md` when the pass is done.
- Run `pnpm run verify` to ensure build passes.
